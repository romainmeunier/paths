version: 2
jobs:
  build:
    docker: # See https://docs.docker.com/get-started/#docker-concepts if you are new to Docker.
      - image: circleci/node:8
    steps:
      - checkout
      - run:
          name: 'yarn install'
          command: 'yarn install'
      - run:
          name: 'build'
          command: 'yarn run webpack:build'

